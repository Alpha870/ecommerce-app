{
  "version": 3,
  "builds": [
    {
      "src": "/server/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/server/apis/products" }
  ]
}